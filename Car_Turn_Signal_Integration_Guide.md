# 小车转弯信号集成指南

## 功能概述

本功能实现了STM32F407VET6系统接收循迹小车转弯信号，并在模式3下自动执行X轴向右转90度的功能。

## 硬件连接

### 引脚连接
- **STM32F407VET6 PC7 (USART6_RX)** ← **MSPM0G3507 PA0 (TX)**
- **STM32F407VET6 PC6 (USART6_TX)** → **MSPM0G3507 PA1 (RX)**
- **GND** ↔ **GND**

### 通信参数
- 波特率：9600
- 数据位：8位
- 停止位：1位
- 校验位：无

## 代码修改说明

### 1. USART6配置修改
- 波特率从115200改为9600以匹配小车
- 启用DMA接收功能
- 添加接收缓冲区`car_rx_buf[64]`

### 2. 新增功能函数
```c
void process_car_turn_signal(char* data, uint16_t size)
```
- 解析接收到的转弯信号
- 检查当前系统模式
- 在模式3下执行X轴90度右转
- 发送确认信息回小车

### 3. 信号处理逻辑
1. 接收小车发送的"TURN_START\r\n"信号
2. 检查当前系统模式
3. 如果是模式3，执行`Step_Motor_Rotate_X_Angle(90)`
4. 等待接收小车下一次转弯信号

## 工作流程

### 小车端
1. L2传感器检测到转弯线
2. 发送"TURN_START\r\n"到STM32
3. 继续循迹运行

### STM32端
1. USART6接收转弯信号
2. 解析"TURN_START"字符串
3. 检查当前模式（必须是模式3）
4. 执行X轴向右转90度
5. 等待接收下一次转弯信号

## 测试步骤

### 1. 硬件测试
1. 连接STM32和小车的串口线
2. 确保GND连接正确
3. 上电测试通信

### 2. 软件测试
1. 将STM32切换到模式3
2. 手动触发小车L2传感器
3. 观察STM32串口输出
4. 确认X轴电机转动90度

### 3. 集成测试
1. 让小车正常循迹
2. 观察转弯时STM32的响应
3. 验证X轴转动时机正确
4. 确认系统能连续接收多次转弯信号

## 调试信息

系统会通过UART1输出详细的调试信息：

```
=== CAR TURN SIGNAL RECEIVED ===
Received: TURN_START
Current system mode: 3
Mode 3 detected - Executing X-axis 90° right turn
=== MOTOR CONTROL START ===
Input angle: 90 degrees
Direction: CW (Right), Angle: +90
Calculated rotation time: 600 ms
X-axis 90° right rotation completed
=== CAR TURN SIGNAL PROCESSING COMPLETE ===
```

## 注意事项

1. **模式限制**：只有在模式3下才会响应转弯信号
2. **防抖处理**：系统自动处理重复信号
3. **错误处理**：无效信号会被忽略
4. **时序控制**：电机转动有适当的延时
5. **状态反馈**：详细的串口调试信息
6. **单向通信**：只接收小车信号，不发送确认信息

## 故障排除

### 问题1：收不到转弯信号
- 检查硬件连接
- 确认波特率设置（9600）
- 检查小车是否正常发送

### 问题2：收到信号但不转动
- 确认当前是否为模式3
- 检查电机初始化状态
- 查看串口调试信息

### 问题3：只能响应第一次转弯信号
- **已修复**：USART6_IRQHandler中添加了DMA重启代码
- 确保与其他UART中断处理保持一致
- 现在支持连续多次转弯信号

### 问题4：转动角度不准确
- 检查电机参数配置
- 确认脉冲计算公式
- 调整电机速度和加速度

## 扩展功能

可以根据需要添加以下功能：
1. 支持不同角度的转动
2. 支持其他模式下的响应
3. 添加转动方向选择
4. 实现更复杂的通信协议
