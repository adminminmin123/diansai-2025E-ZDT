# 步进电机角度控制功能实现总结

## 实现概述
已成功实现按键控制步进电机二维云台X轴按指定角度旋转的功能。当按下对应按键时，电机会向相应方向旋转指定角度。

## 修改的文件

### 1. bsp/key_bsp.h
**新增内容:**
- 角度控制函数声明
- 角度常量定义 (45°, 90°, 135°, 180°)
- 按键值定义 (KEY_LEFT_45 到 KEY_RIGHT_135)

### 2. bsp/key_bsp.c
**新增内容:**
- 角度按键状态变量
- `read_angle_keys()` 函数：读取所有角度控制按键
- `key_motor_angle_control()` 函数：处理角度控制逻辑
- 在 `key_proc()` 中集成角度控制处理

### 3. bsp/step_motor_bsp.h
**新增内容:**
- `Step_Motor_Rotate_X_Angle()` 函数声明

### 4. bsp/step_motor_bsp.c
**新增内容:**
- `Step_Motor_Rotate_X_Angle()` 函数实现
- 角度到脉冲数的转换逻辑
- 方向控制和调试信息输出

## 按键映射

| 物理按键 | 引脚 | 功能描述 | 旋转角度 |
|---------|------|---------|---------|
| Left_45 | PB8  | 左转45度 | -45° |
| Left_90 | PB6  | 左转90度 | -90° |
| Left_135| PB4  | 左转135度| -135°|
| Left_180| PE0  | 左转180度| -180°|
| Right_45| PC3  | 右转45度 | +45° |
| Right_90| PC1  | 右转90度 | +90° |
| Right_135|PE2  | 右转135度| +135°|

## 技术细节

### 角度计算
- 步进电机: 200步/圈，16细分
- 每度脉冲数: (200 × 16) ÷ 360 ≈ 8.89 脉冲/度
- 使用四舍五入确保精度

### 防抖机制
- 角度控制按键: 200ms防抖时间
- 防止按键重复触发导致的意外旋转

### 方向控制
- 正角度值: CW方向 (右转)
- 负角度值: CCW方向 (左转)
- 自动处理方向位和脉冲数计算

## 集成方式

### 调度系统集成
功能已集成到现有的调度系统中：
- `key_proc()` 每20ms执行一次
- 角度控制处理在传统按键处理之前执行
- 不影响现有的激光控制功能

### 调试输出
通过UART1输出详细的调试信息：
```
Angle Key Pressed: X, Rotating X-axis by Y degrees
X Motor: Rotating Y degrees, dir=Z, pulses=W
```

## 使用流程

1. **系统启动**: 电机自动初始化
2. **按键操作**: 按下任意角度控制按键
3. **防抖处理**: 系统进行200ms防抖
4. **角度计算**: 根据按键计算旋转角度和脉冲数
5. **电机控制**: 发送位置控制命令给X轴电机
6. **调试输出**: 通过UART1输出操作信息

## 兼容性

### 现有功能保持
- 激光控制功能 (KEY1/KEY2) 保持不变
- PID控制系统保持不变
- UART通信系统保持不变
- 调度系统保持不变

### 扩展性
- 可轻松添加新的角度值
- 可扩展到Y轴控制
- 可添加组合按键功能

## 测试建议

1. **单独测试**: 逐个测试每个角度按键
2. **连续测试**: 测试连续按键的响应
3. **边界测试**: 测试最大角度(±180°)
4. **防抖测试**: 快速连续按键测试防抖效果
5. **调试输出**: 监控UART1输出确认功能正常

## 注意事项

1. **电机限制**: 确保电机机械结构支持大角度旋转
2. **电源要求**: 大角度旋转可能需要更多电流
3. **初始位置**: 建议在已知位置开始测试
4. **安全考虑**: 测试时确保云台周围无障碍物

## 后续优化建议

1. **位置反馈**: 添加编码器反馈确认实际旋转角度
2. **速度控制**: 根据角度大小调整旋转速度
3. **平滑控制**: 添加加速度控制实现平滑启停
4. **位置记忆**: 记录当前位置，支持相对和绝对定位
5. **Y轴扩展**: 实现Y轴的角度控制功能
